<%= render "home/header" %>
<div class="row">
<%= render "home/navbar" %>

      <div class="col s12 m12 l9"> <!-- Note that "m8 l9" was added -->
            <div class="divider"></div>
            <div>
                <h5 class="center-align white-text">Buscador SKU</h5></a>  
            </div>
            <div class="divider"></div>

            <div class="nav-wrapper white-text row">
              <%= form_tag({:controller => "product/search", :action => "index"}, :method => :get) do %>

  			      <%= text_field_tag "searchbox", params[:searchbox], placeholder: "      Ingrese el SKU del producto", :class => "black white-text" %>
              <%= submit_tag "Buscar", :name => nil , :class => "waves-effect waves-light btn" do %>

              <%end%>
  			    

			      <% end %>

            </div>

            <%if @product != nil and @product != 'a' and @product != 'e' and @product != '-' and @product != 'i'%>
            <div class="row">
          	  <div class="col s12 m12">
              	<div class="card blue-grey darken-1">
                <div class="card-content white-text">
                  <span class="card-title">Información obtenida en tiempo real Consulta Web Service:</span>
                  <p>
                  <span>
                  <p> 
                  <strong>Identificador de producto: </strong>
                  <%= @product.product_identifier%> 
                  </p>
                  <p> 
                  <strong>Precio normal: </strong> 
                  <%= @product.product_normal_price%> 
                  </p>
                  <p> 
                  <strong>Precio mínimo: </strong> 
                  <%= @product.product_minimum_price%> 
                  </p>
                   <p> 
                   <strong>Stock: </strong> 
                   <%=@product.product_stock%> 
                   </p>
                  </span>
                  </p>
                </div>
                <div class="card-action center">
                  <% url = Datum::API_BASE_URL_PRODUCT_ALL+" and pt_part=\""+@product.product_identifier+"\""%>
                  <a href="<%= url %>">IR ERP ITAKA</a>
                </div>
              </div>
          	</div>
      		</div>
          
          <%elsif @product == 'a'%>
            <span class="white-text">Producto no encontrado</span>
          <%elsif @product == 'e'%>
          <span class="white-text">Problemas con ERP</span>
          <%elsif @product == 'i'%>
          <span class="white-text">Ingrese parámetros en la búsqueda</span>
      		<%end%>
            
      </div>

</div>
<%= render "home/footer" %>

<script type="text/javascript">
  if window.location.href == 
</script>