<%= render "home/header" %>
<div class="row">
<%= render "home/navbar" %>

      <div class="col s9 m9 l9 white"> <!-- Note that "m8 l9" was added -->
            <div class="divider"></div>
            <div>
                <h5 class="center-align">Buscador SKU</h5></a>  
            </div>
              
            <div class="nav-wrapper white black-text">
              <%= form_tag({:controller => "product/search", :action => "index"}, :method => :get) do %>

			    <%= text_field_tag "searchbox", params[:searchbox], placeholder: "      Ingrese el SKU del producto", :class => "black white-text" %>

			    <%= submit_tag "Buscar", :name => nil , :class => "waves-effect waves-light btn"%>

			  <% end %>
            </div>
            
            <%begin%>
	            <% if @products.length != 0%>
	            <div class="section">
	                        <div class="row">
					        <div class="col s12 m6">
					          <div class="card blue-grey darken-1">
					            <div class="card-content white-text">
					              <span class="card-title">Información producto</span>
					              <%=@products%>
					            </div>
					          </div>
					         </div>
					        </div>
				 </div>
				 <%else%>
				  No hay resultados
	            <%end%>
            <%rescue Exception => e%>
              No hay resultados
            <%end%>
            
      </div>

</div>
<%= render "home/footer" %>