<%= form_for(@team_report) do |f| %>
  <% if @team_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_report.errors.count, "error") %> prohibited this team_report from being saved:</h2>

      <ul>
      <% @team_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
