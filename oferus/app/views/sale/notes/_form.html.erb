<%= form_for(@sale_note) do |f| %>
  <% if @sale_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_note.errors.count, "error") %> prohibited this sale_note from being saved:</h2>

      <ul>
      <% @sale_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
