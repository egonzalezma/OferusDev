<%= form_for(@sale_flow) do |f| %>
  <% if @sale_flow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_flow.errors.count, "error") %> prohibited this sale_flow from being saved:</h2>

      <ul>
      <% @sale_flow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
