<%= form_for(@sale_note_item) do |f| %>
  <% if @sale_note_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_note_item.errors.count, "error") %> prohibited this sale_note_item from being saved:</h2>

      <ul>
      <% @sale_note_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
