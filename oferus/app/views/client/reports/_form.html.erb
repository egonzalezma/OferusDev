<%= form_for(@client_report) do |f| %>
  <% if @client_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_report.errors.count, "error") %> prohibited this client_report from being saved:</h2>

      <ul>
      <% @client_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
